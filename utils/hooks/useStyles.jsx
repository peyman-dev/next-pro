import React from "react";
import classNames from "classnames";

const useStyles = () => {

  const labelStyles = classNames(
    "font-YekanBakh-Medium text-sm mb-1 flex items-center gap-1"
  );

  return {
    labelStyles,
  };
};

export default useStyles;
